<section>
  <ol>
    <% @found_pet.each do |pet| %>
      <li id="found_<%= pet.name %>"><%= pet.name %><%= button_to "Adopt this Pet", {controller: :application_pets, pet_id: pet.id, application_id: @application.id, action: :create} %></li>
    <% end %>
  </ol>
</section>